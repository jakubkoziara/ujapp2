<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.hidden_field :commenter, value: current_user.try(:email) %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
